import { SxProps, Theme } from '@mui/material/styles';

// Box styles for TabPanel content
export const tabPanelBoxStyles: SxProps<Theme> = {
  p: { xs: '2px', md: 3 },
  overflow: 'hidden',
  width: '100%',
  minWidth: 0,
};
